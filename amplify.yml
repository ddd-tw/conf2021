version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 16.3.0
        - nvm use 16.3.0
        - npm i -g pnpm@6.7.1
        - pnpm i
    build:
      commands:
        - pnpm build
  artifacts:
    baseDirectory: build
